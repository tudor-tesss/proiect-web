export class User {
    constructor(id, name, firstName, emailAddress) {
        this.id = id;
        this.name = name;
        this.firstName = firstName;
        this.emailAddress = emailAddress;
    }
}